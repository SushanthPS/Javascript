/* 
    Simple interest formula is
    SI = (P*T*R)/100
*/

var p=100000; //principal amount
var r=8; //rate percent
var t=5; //time in years

var simpleInterest = (p*t*r)/100;

console.log(simpleInterest);
